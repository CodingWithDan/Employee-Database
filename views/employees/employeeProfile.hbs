<div class="row">
    <div class="col s12 m8">
        <h3>{{employee.fullName}}
        </h3>
        <div class="card story">
            <div class="card-content">
                <span class="card-title">Last Updated: {{formatDate date 'MMMM Do YYYY'}}</span>
            </div>
        </div>
    </div>

  
    <table class="striped">
    <thead>
        <tr>
            <th>Production</th>
            <th>Start Date</th>
        </tr>
    </thead>
    <tbody>

        <tr>
            <td><a href="/employees/viewInfo/{{employee._id}}">{{employee.production}}</td>
            <td>{{employee.firstWorkDay}}</td>
            <td>
                <a href="/employees/edit/{{employee._id}}" class="btn btn-float">
                    <i class="fas fa-edit"></i>
                </a>

               <form action="/employees/{{_id}}" method="POST" id="delete-form">
                    <input type="hidden" name="_method" value="DELETE">
                    <button type="submit" class="btn red">
                        <i class="fas fa-trash"></i>
                    </button>
                </form>
            </td> 
        </tr>

        {{!-- <tr>
            <td><a href="/employees/viewI9/{{employee._id}}">I9</a></td>
            <td>{{I9.i9done}}</td>
            <td>
                <a href="/employees/addi9/{{employee._id}}" class="btn btn-float">
                    <i class="fas fa-plus"></i>
                </a>

                <a href="/employees/editi9/{{employee._id}}" class="btn btn-float">
                    <i class="fas fa-edit"></i>
                </a>

                <form action="/employees/{{_id}}" method="POST" id="delete-form">
                    <input type="hidden" name="_method" value="DELETE">
                    <button type="submit" class="btn red">
                        <i class="fas fa-trash"></i>
                    </button>
                </form>
            </td> 
        </tr>

        <tr>
            <td><a href="/employees/viewW4/{{w4._id}}">W4</td>
            <td>{{w4.w4done}}</td>
            <td>
                 <a href="/employees/addw4/{{employee._id}}" class="btn btn-float">
                    <i class="fas fa-plus"></i>
                </a>
                <a href="/employees/editw4/{{employee._id}}" class="btn btn-float">
                    <i class="fas fa-edit"></i>
                </a>

                <form action="/employees/{{_id}}" method="POST" id="delete-form">
                    <input type="hidden" name="_method" value="DELETE">
                    <button type="submit" class="btn red">
                        <i class="fas fa-trash"></i>
                    </button>
                </form>
            </td> 
        </tr> --}}
       
    </tbody>
</table>


</div>




 {{!-- <td>
                <a href="/stories/edit/{{_id}}" class="btn btn-float">
                    <i class="fas fa-edit"></i>
                </a>

                <form action="/stories/{{_id}}" method="POST" id="delete-form">
                    <input type="hidden" name="_method" value="DELETE">
                    <button type="submit" class="btn red">
                        <i class="fas fa-trash"></i>
                    </button>
                </form>


                <small>{{{editIcon story.user user story._id false}}}</small>
            </td> --}}